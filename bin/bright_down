#!/bin/bash

brightness=$(cat /sys/class/backlight/intel_backlight/brightness)

new_bright=$(($brightness - 200))

if (($new_bright < 0)); then
    let brightess=0
fi

echo $new_bright > /sys/class/backlight/intel_backlight/brightness
