#!/bin/sh

git_directory=$(git rev-parse --show-superproject-working-tree 2> /dev/null)

if [ -z "$git_directory" ]; then
    git_directory=$(git rev-parse --show-toplevel 2> /dev/null)
fi

if [ -z "$git_directory" ]; then
    git_directory=$(pwd)
fi

echo $git_directory
